#!/usr/bin/env perl

use warnings;
use strict;

use Test::More tests => 5;

BEGIN {
	use_ok( 'Police::Dispatch' );
	use_ok( 'Police::Dispatch::Officer' );
	use_ok( 'Police::Dispatch::Crew' );
	use_ok( 'Police::Dispatch::Call' );
	use_ok( 'Police::Dispatch::CrewCall' );
}

diag( "Testing Police::Dispatch $Police::Dispatch::VERSION, Perl $], $^X" );
